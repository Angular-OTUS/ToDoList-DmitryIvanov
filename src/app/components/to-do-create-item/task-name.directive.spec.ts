import { TaskNameDirective } from './task-name.directive';

describe('TaskNameDirective', () => {
  it('should create an instance', () => {
    const directive: TaskNameDirective = new TaskNameDirective();
    expect(directive).toBeTruthy();
  });
});
